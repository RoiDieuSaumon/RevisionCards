<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activity.CardsManagerActivity">

    <include
        layout="@layout/toolbar"
        android:id="@+id/activity_cards_manager_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/activity_cards_manager_add_subject_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:text="@string/Add_subject"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:width="210dp"
        android:height="70dp"
        android:textSize="20sp" />

    <FrameLayout
        android:id="@+id/activity_cards_manager_cards_tree_view_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/activity_cards_manager_toolbar"
        android:layout_above="@+id/activity_cards_manager_add_subject_btn" />

</RelativeLayout>